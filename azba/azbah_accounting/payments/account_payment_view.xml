<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- VIEWS -->

        <record id="azbah_view_account_payment_tree" model="ir.ui.view">
            <field name="name">azbah.account.payment.tree</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_tree"/>
            <field name="mode">primary</field>
            <field name="arch" type="xml">
                <field name="partner_id" position="attributes">
                    <attribute name="string">المستلم</attribute>
                </field>
            </field>
        </record>


        <record id="azbah_view_account_payment_form" model="ir.ui.view">
            <field name="name">azbah.account.payment.form</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_form"/>
            <field name="mode">primary</field>
            <field name="arch" type="xml">
                <xpath expr="//group[@name='group1']" position="replace">
                    <group name="group1">
                        <field name="is_internal_transfer" invisible="1"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        <field name="payment_type" invisible="1" widget="radio" options="{'horizontal': True}"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        <field name="partner_id" context="{'default_is_company': True}" string="المستلم"
                               attrs="{'readonly':[('state', '!=', 'draft')]}"/>
                        <label for="amount"/>
                        <div name="amount_div" class="o_row">
                            <field name="amount"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            <field name="currency_id"
                                   options="{'no_create': True, 'no_open': True}"
                                   required="1"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"
                                   groups="base.group_multi_currency"/>
                        </div>
                        <field name="date"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        <field name="ref" string="Memo"/>
                    </group>
                </xpath>

            </field>
        </record>


        <!-- ACTIONS -->

        <!--        سند صرف         -->
        <!--        Receive         -->
        <!--                        -->

        <record id="action_azbah_account_payments_payable" model="ir.actions.act_window">
            <field name="name">Payments</field>
            <field name="res_model">account.payment</field>
            <field name="context">{
                'default_payment_type': 'outbound',
                'search_default_outbound_filter': 1,
                'default_move_journal_types': ('bank', 'cash'),
                'sanad':'1'
                }
            </field>
            <field name="view_id" ref="azbah_view_account_payment_tree"/>
        </record>


        <record model="ir.actions.act_window.view" id="action_azbah_account_payment_tree">
            <field name="sequence" eval="1"/>
            <field name="view_mode">tree</field>
            <field name="view_id" ref="azbah_view_account_payment_tree"/>
            <field name="act_window_id" ref="action_azbah_account_payments_payable"/>
        </record>

        <record model="ir.actions.act_window.view" id="action_azbah_account_payment_form">
            <field name="sequence" eval="3"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="azbah_view_account_payment_form"/>
            <field name="act_window_id" ref="action_azbah_account_payments_payable"/>
        </record>


        <!--        سند قبض         -->
        <!--        Receive         -->
        <!--                        -->

        <record id="action_azbah_account_payments_receive" model="ir.actions.act_window">
            <field name="name">Payments</field>
            <field name="res_model">account.payment</field>
            <field name="context">{
                'default_payment_type': 'inbound',
                'search_default_inbound_filter': 1,
                'default_move_journal_types': ('bank', 'cash'),
                'sanad':'1'
                }
            </field>
            <field name="view_id" ref="azbah_view_account_payment_tree"/>
        </record>


        <record model="ir.actions.act_window.view" id="action_azbah_account_payment_tree_receive">
            <field name="sequence" eval="1"/>
            <field name="view_mode">tree</field>
            <field name="view_id" ref="azbah_view_account_payment_tree"/>
            <field name="act_window_id" ref="action_azbah_account_payments_receive"/>
        </record>

        <record model="ir.actions.act_window.view" id="action_azbah_account_payment_form_receive">
            <field name="sequence" eval="3"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="azbah_view_account_payment_form"/>
            <field name="act_window_id" ref="action_azbah_account_payments_receive"/>
        </record>


        <menuitem parent="account_accountant.menu_accounting"
                  id="menu_azbah_account"
                  groups="account.group_account_user"
                  name="حركات مالية"
                  sequence="100"/>

        <menuitem parent="menu_azbah_account"
                  id="menu_azbah_action_account_payments_send"
                  action="action_azbah_account_payments_payable"
                  groups="account.group_account_user"
                  name="سند صرف"
                  sequence="0"/>

        <menuitem parent="menu_azbah_account"
                  id="menu_azbah_action_account_payments_receive"
                  action="action_azbah_account_payments_receive"
                  groups="account.group_account_user"
                  name="سند قبض"
                  sequence="0"/>

    </data>
</odoo>
